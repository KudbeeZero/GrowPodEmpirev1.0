# Local Wrangler Configuration
# Copy this file to wrangler.local.toml and fill in your actual values
# wrangler.local.toml is gitignored to keep secrets safe
#
# Note: Wrangler does NOT automatically merge wrangler.local.toml with wrangler.toml.
# To use this local config, run Wrangler with the -c flag, for example:
#   wrangler dev -c wrangler.local.toml
#   wrangler deploy -c wrangler.local.toml
#
# IMPORTANT: This example file only contains sensitive fields.
# For a complete local config, copy wrangler.toml to wrangler.local.toml and edit it,
# or use the --account-id flag: wrangler deploy --account-id YOUR_ACCOUNT_ID

account_id = "YOUR_CLOUDFLARE_ACCOUNT_ID"

[[d1_databases]]
binding = "DB"
database_name = "growpod-empire-db"
database_id = "YOUR_D1_DATABASE_ID"
